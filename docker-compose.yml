services:
  fintrack:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - DATABASE_URL=file:/app/data/fintrack.db
    volumes:
      - fintrack-data:/app/data
    restart: unless-stopped

volumes:
  fintrack-data:
